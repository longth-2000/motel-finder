export const getters = {
    chat: (state) => {
        return state.chat
    },
    conversation(state) {
        return state.conversation
    },
    isOpen(state) {
        return state.isOpen
    }
}